1. Caret, selection and scrolling is moved outside of SpaceView.
2. Block position and width is saved when changes.
3. View will autometically scroll when mouse falls out of the window when selecting. But problem is when mouse move stops it won't scroll, may need a timer.